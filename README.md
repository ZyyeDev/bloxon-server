# This is basically the server for Bloxon.
To set this up you must use hetzner, clone this repository into root and just ./deploy.sh and you're done.
You'll also need to set the token.

*TODO: just use .env for everything, not enviorement variables*
